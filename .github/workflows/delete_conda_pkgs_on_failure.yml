name: Build Cleanup of Anaconda Cloud Packages on Failure

on:
  schedule:
    - cron: '0 0 * * 0' # Runs every Sunday at midnight
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6

      - name: Conda environment creation and activation
        uses: conda-incubator/setup-miniconda@main
        with:
          conda-remove-defaults: true
          python-version: 3.12
          activate-environment: fermitools-cleanup
          environment-file: environments/fermitools-cleanup.yml
          auto-update-conda: false
          auto-activate-base: false
          show-channel-urls: true

      # - name: Install Anaconda Client
      #   run: conda install -y anaconda-client 

      # - name: Authenticate Anaconda
        # run: anaconda login --token ${{ secrets.ANACONDA_TOKEN }}
        # anaconda login --username ${{ secrets.ANACONDA_USER }} --password ${{ secrets.ANACONDA_PASSWORD }}

      - name: Anaconda Login and Upload
        env:
          ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_TOKEN }}
        run: |
          # The anaconda client automatically reads ANACONDA_API_TOKEN for authentication
          anaconda token install ${{ secrets.ANACONDA_TOKEN }}
          anaconda login --token ${{ secrets.ANACONDA_TOKEN }}
          anaconda whoami
          
          # Optional: Use anaconda upload to publish your package
          # anaconda upload <path-to-package.tar.bz2>

      - name: Delete Old Packages
        # Example using a custom script or anaconda-client to prune builds
        # anaconda remove -y your-channel/your-package/$version
        run: |
          # Replace 'your-channel/your-package' with your exact entity
          # This script example removes versions beyond the latest 5
          anaconda show fermi/fermitools | grep -oE 'version: [0-9.]+' | tail -n +6 | while read -r ver; do
            version=$(echo "$ver" | awk '{print "$2"}')
            echo "$version"
          done

      - name: Logout Anaconda
        run: anaconda logout

